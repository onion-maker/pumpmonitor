# 更新紀錄

## v1.8.0 (2026-07-27)

### 修復抽水機警報漏報問題（方案 B）
- **問題**：30 秒 snapshot 輪詢方式，如果抽水機在兩次輪詢之間完成啟停（啟動後 30 秒內又停止），snapshot 比對會看到 before=0, after=0，完全遺漏這段狀態變化
- **解法**：改用 `GetAutoPumpWaterMins` 歷史 API，每次輪詢取得過去 2 分鐘內所有紀錄，對每對相鄰 record 逐一比對 16 台水泵和 16 個閘門的狀態變化
- **影響檔案**：
  - `src/api/pumpStation.ts` — TideRecord 介面新增 `pumps` 欄位，新增 `fetchPumpHistoryForStations()` 批次歷史查詢函式
  - `src/hooks/usePumpData.ts` — 在 `fetchData` 主輪詢中整合歷史 API 呼叫與 `checkPumpHistoryAlarm()`
  - `src/store/slices/alarmSlice.ts` — 新增 `checkPumpHistoryAlarm()` 方法，逐對比對相鄰紀錄偵測啟停變化，含去重機制（`alarmedPumpEvents` / `alarmedDoorEvents`）

### 修復漲潮通知從未觸發問題
- **問題**：漲潮條件使用浮點數精確相等比較（`pi2_lo === pi_lo && pi_lo === ni_lo` 或 `pi2_lo === pi_lo && ni_lo > pi_lo`），水位值幾乎不可能完全相等，導致漲潮關閉通閥通知從未觸發
- **解法**：改為趨勢判斷，計算尾兩筆均值 vs 頭兩筆均值：`tailAvg > headAvg`，只要趨勢向上就算漲潮，用趨勢代替精確值比較更符合實際情況
- **影響檔案**：
  - `src/store/slices/tideSlice.ts`：漲潮條件改用趨勢計算
  - `android/.../PumpMonitorService.java`：Java 背景服務同步修改，改用相同趨勢演算法

### 修復潮汐警報延遲 1 小時問題
- **問題**：潮汐檢查間隔原本是 10 分鐘，錯過快速變化的漲潮時機，通常漲潮過了一小時甚至更久才收到通知
- **解法**：將 `TIDE_CHECK_INTERVAL_MS` 從 10 分鐘縮短為 90 秒，確保在漲潮發生時能即時捕捉變化
- **影響檔案**：`src/hooks/usePumpData.ts`

### 修復潮汐警報覆蓋其他警報問題
- **問題**：`updateTide()` 使用直接 `set()` 替換整個 `alarmingStations`，導致潮汐檢查時覆蓋掉 `checkAlarm()` 產生的水泵/水位警報
- **解法**：改用函數式 `set((s) => ...)` 更新，在合併時保留現有的非潮汐警報
- **影響檔案**：`src/store/slices/tideSlice.ts`
- **附帶修復**：`src/store/slices/tideSlice.ts` 檔名在記錄中誤植為 `tideSli.ts`（缺 `ce`），實際檔名為 `tideSlice.ts`

## v1.8.1 (2026-07-27)

### 修復新生站閘門檢查漏掉 door01
- **問題**：`TIDE_DOOR_COLS['112']` 只包含 `door02~05`，漏掉了 `door01`，導致新生站 1 號閘門的啟閉狀態不被潮汐警報檢查
- **解法**：將 `door01` 加入新生站閘門清單
- **影響檔案**：`src/config/stations.ts`

## v1.8.2 (2026-07-27)

### 閘門啟閉紀錄改進
- **問題**：潮汐站卡片上看不到閘門啟閉紀錄按鈕，且半開狀態(`'2'`)的閘門過渡不會產生操作紀錄
- **解法**：
  - 操作紀錄按鈕改為永遠顯示（無紀錄時打開 modal 顯示「暫無紀錄」）
  - 閘門偵測覆蓋半開狀態：關→半開/全開記錄為「開啟」，半開/全開→關記錄為「關閉」
  - 潮汐警報（退潮開閘／漲潮關閘）事件也寫入閘門操作紀錄，標註「(潮汐建議)」
  - 歷史 API 時間窗口從 2 分鐘拉大到 10 分鐘，提高捕捉變化機率
- **影響檔案**：
  - `src/components/StationCard.tsx` — 按鈕永遠顯示
  - `src/components/OperationLogs.tsx` — 閘門記錄顯示 `(潮汐建議)` 標註
  - `src/store/slices/alarmSlice.ts` — 閘門偵測覆蓋 `'2'` 狀態
  - `src/store/slices/tideSlice.ts` — 潮汐警報同時寫入 gateOperationLog
  - `src/types/index.ts` — GateOperationLogEntry 新增 `source?: 'tide'`
  - `src/hooks/usePumpData.ts` — 歷史 API 查詢從 2 分鐘擴為 10 分鐘

## v1.8.3 (2026-07-28)

### 新增潮汐方向變化紀錄
- **功能**：獨立於抽水機／閘門操作紀錄，新增潮汐方向變化紀錄（TideLogEntry）
- **記錄時機**：每次 `updateTide` 偵測到方向變化（漲↔退↔平）時寫入一筆
- **UI**：
  - 潮汐站卡片新增「潮汐紀錄」按鈕（在操作紀錄右邊）
  - Modal 改為三 tab：抽水機／閘門／潮汐
  - 設定頁 OperationLogs 也新增「潮汐紀錄」tab
  - 方向變化以顏色標示：退潮（藍）、漲潮（紅）
- **持久化**：`tideOperationLog` 儲存於 localStorage
- **影響檔案**：
  - `src/types/index.ts` — 新增 `TideLogEntry` 型別
  - `src/store/slices/tideSlice.ts` — 新增 `tideOperationLog` 狀態、`getTideLogsByStation()`、`clearTideLogs()`
  - `src/components/StationCard.tsx` — 潮汐紀錄按鈕 + tab UI
  - `src/components/OperationLogs.tsx` — 設定頁潮汐紀錄 tab
  - `src/store/slices/authSlice.ts`、`src/store/useStore.ts` — persist 支援

## v1.8.4 (2026-07-28)

### 潮汐紀錄防抖動（two-confirmation）
- **問題**：API 水位資料 5 分鐘更新一次，但潮汐檢查 90 秒一次，導致方向判斷在相鄰週期來回跳動，一天可能產生數百筆假變化記錄
- **解法**：增加連續兩次確認機制（`_pendingTideChange`），新方向需在下一次 `updateTide` 時再次確認一致，才正式寫入 `tideOperationLog`
- **效果**：一天約 4 筆潮汐方向變化記錄（對應 2 次漲潮、2 次退潮）
- **影響檔案**：`src/store/slices/tideSlice.ts`