 app啟動後潮汐紀錄不會補上

Let me investigate the tide data flow to understand why tide records aren't being populated on app startup.

  Read 3 files

Now let me look at the fetchTideRecords function and the recordLevelOut usage to understand the full flow.

  Searched for 1 pattern, read 1 file

Now I see the issue clearly. Let me check recordLevelOut calls to confirm.

  Searched for 1 pattern

recordLevelOut is defined in the store but never called anywhere. The tide buffer is built from recordLevelOut which is supposed to be called during polling — but it's never invoked. Meanwhile, updateTide is called with fetchTideRecords() results which pull from the GetAutoPumpWaterMins API directly.

The problem is clear: when the app first starts (cold start), the tideDirection is empty (line 101 in usePumpData.ts), so hasPrevDir is false and hoursBack is set to 12. But looking at the updateTide logic in tideSlice.ts:135 — the cold start path (prevDir is undefined)

  Searched for 1 pattern

Let me check if there's a tide operation log display in the UI, and verify the cold start path more carefully.

  Searched for 1 pattern, read 1 file

Now I understand the full picture. Here's the bug:

Root cause: In usePumpData.ts:101-102, when the app starts cold (no previous tide direction):

const hasPrevDir = Object.keys(state.tideDirection).length > 0;
const hoursBack = hasPrevDir ? 3 : 12;

It correctly requests 12 hours of data, and fetchTideRecords fetches from the GetAutoPumpWaterMins API. Then updateTide is called with the results.

In tideSlice.ts:130-163, the cold start path (when prevDir is undefined) scans the 12-hour records window-by-window to find reversals and build tideOperationLog. But the problem is in the scan range:

const win = 5;
for (let i = win; i <= records.length; i += 1) {

When the API returns a lot of records (12 hours worth), this sliding window scan over potentially hundreds of records should find reversals. Let me check if the API actually returns data.

Let me check the raw tide API call to see if it might be failing silently.

  Read 1 file

Now let me check the RectimeToMs format for this API — the cold start scan timestamps need to match.

Let me verify the rectime format coming from fetchTideRecords. The TideApiResponse uses RawStationData which I need to check the rectime format.

  Searched for 1 pattern

OK, so rectime in the TideRecord is yyyyMMddHHmmss string. The rectimeToMs function in tideSlice.ts:76 parses this format. That looks correct.

Now let me understand the actual problem. The user said "潮汐紀錄不會補上" — the tide records don't appear after app starts. Let me trace through the cold start path more carefully.

Looking at tideSlice.ts:135-162:

} else {
  const existingLogs = state.tideOperationLog.filter(l => l.stationNo === stationNo);
  const lastLoggedTime = existingLogs.length > 0
    ? Math.max(...existingLogs.map(l => l.timestamp))
    : 0;

  const win = 5;
  let lastDir: TideDirection | null = null;
  let logCount = 0;
  for (let i = win; i <= records.length; i += 1) {
    const slice = records.slice(i - win, i);
    const time = rectimeToMs(records[i - 1].rectime);
    if (time <= lastLoggedTime) {
      const d = detectOnSlice(slice);
      if (d) lastDir = d;
      continue;
    }
    const dir = detectOnSlice(slice);
    if (!dir) continue;
    if (lastDir !== null && dir !== lastDir) {
      const revIdx = findReversalInRange(records, i - win, i - 1, dir);
      newTideLog.push({ timestamp: rectimeToMs(records[revIdx].rectime), stationNo, from: lastDir, to: dir });
      logCount++;
    }
    lastDir = dir;
  }

And detectOnSlice (line 82-93):
const detectOnSlice = (records: TideRecord[]): TideDirection | null => {
  const valid = records.map(r => r.level_out).filter(v => v !== null) as number[];
  if (valid.length < 2) return null;
  // ...compare
  if (dec > inc) return 'falling';
  if (inc > dec) return 'rising';
  return 'slack';
};

The issue is: if the API returns records where level_out is frequently null or only has 1-2 valid values per 5-record window, `det

  Read 1 file

OK, I have a good understanding now. Let me also check the reversalCheck for 新生/中山 — line 131-133:

if (prevDir) {
  if (prevDir !== newDir) {
    const revIdx = findReversalIdx(records, newDir);
    newTideLog.push({ ... });
  }
}

This only pushes if prevDir !== newDir. On the first poll, prevDir is undefined, so it falls to the else (cold start path). The cold start path scans all 12 hours and should populate logs.

But then on subsequent polls (3 hours of records), prevDir exists from the cold start scan. If there hasn't been a tide direction change within the last 3 hours, findReversalIdx will return 0 (no reversal found), and no log would be added. But the user is saying they don't see any records even on first start.

Let me check if there's a console.warn being hit — specifically around line 54 or 56 where it returns early if records are too few.

The real question: does fetchTideRecords actually return data? Let me check if the Android AndroidHttp.fetchPost bridge works correctly for POST requests. Let me look at the debug logs for tide-related entries.

  Searched for 2 patterns

No tide logs in any debug files. Let me check the most recent debug log for anything related to fetchTideRecords or AndroidHttp.

  Searched for 2 patterns, listed 1 directory

The API calls are being made. But notice something concerning: the logs only show "AndroidHttp.fetchPost: URL" — there's no log showing the response or whether it succeeded. Let me check the AndroidHttp bridge implementation.

  Searched for 1 pattern, read 1 file

OK, the Android bridge fetchPost logs the URL and the HTTP response code, but the debug logs show only URL logging, no response codes. That means either the response code logging happened on a different line or the posts failed silently (exception caught by catch block).

Let me look for HTTP POST status/error logs in the debug logs.

  Searched for 1 pattern

So the fetchPost API calls are all returning HTTP 200. The data IS coming back from the server. The problem must be in the processing logic. Let me look more carefully at the cold start scan.

The key issue is in the cold start path (tideSlice.ts:135-162). Look at the first pass through the loop:

let lastDir: TideDirection | null = null;
for (let i = win; i <= records.length; i += 1) {  // win = 5
  const slice = records.slice(i - win, i);
  const time = rectimeToMs(records[i - 1].rectime);
  // ...
  const dir = detectOnSlice(slice);
  if (!dir) continue;           // <-- if dir is null, continues
  if (lastDir !== null && dir !== lastDir) {
    // only pushes when lastDir !== null
  }
  lastDir = dir;
}

When i=5, the first 5 records get analyzed. If detectOnSlice returns a direction, lastDir is set but nothing is pushed (because lastDir was null). The window then advances by 1 (i += 1), but wait — the loop says i += win? No, actually let me re-read: line 145 says i += 1, not i += win.

So the window slides by 1 record each time. That should work fine — on the second iteration, lastDir will be set from the first iteration, and if the direction changed, a log gets pushed.

But wait — the problem might be more subtle. Look at line 148:

if (time <= lastLoggedTime) {
  const d = detectOnSlice(slice);
  if (d) lastDir = d;
  continue;
}

On cold start, lastLoggedTime is 0 since there are no existing logs. So time <= 0 is false for any real record time (which will be in 2026, so timestamp is ~1.7 trillion milliseconds). Good, this won't skip anything.

So the scan should work. But here's what I think might be happening:

The records are sorted by rectime, oldest first. The 12-hour scan gives, say, ~144 records (every 5 min for 12 hours). The scan window slides one record at a time and should detect reversals.

But then on the next poll (30 seconds later), this code runs again because fetchData calls fetchTideRecords(3) (3 hours since hasPrevDir is now true). The updateTide function reaches line 128:

const prevDir = prevDirections[stationNo];
if (prevDir) {
  if (prevDir !== newDir) {
    const revIdx = findReversalIdx(records, newDir);
    if (revIdx > 0) {
      newTideLog.push({ ... });
    }
  }
}

So on each subsequent poll, it only logs if the direction changed from previous. If the tide stays the same, no log entry is added. That's correct behavior.

But wait — the newDirections at line 69-74 use detectTide which only uses the last 5 records. Then prevDir is compared to newDir. If they're same, nothing logged. If different, findReversalIdx sca backwards from the end looking for the point where the direction flipped.

But findReversalIdx at line 96-105 has a problem:

const findReversalIdx = (records: TideRecord[], newDir: TideDirection): number => {
  for (let i = records.length - 1; i >= 1; i--) {
    const a = records[i - 1].level_out;
    const b = records[i].level_out;
    if (a === null || b === null) continue;
    if (newDir === 'rising' && b <= a) return i;
    if (newDir === 'falling' && b >= a) return i;
  }
  return 0;
};

This searches from the end of ALL records (3 hours' worth), looking for the first pair consistent with newDir. For example, if newDir === 'rising', it looks for a pair where b > a. That will likely be found near the end of the record set, and then i is returned.

But the log uses rectimeToMs(records[revIdx].rectime) as the timestamp of the reversal — which would be the time of the first record in the "rising" pair. If the direction changed, say, 5 hours ago (outside the current 3-hour window), findReversalIdx would find this reversal near the end but it would be the current direction appearing, not necessarily where the direction CHANGE occurred.

Actually wait, let me re-think. findReversalIdx is called when prevDir !== newDir — meaning the direction HAS changed. It searches backwards for the first pair consistent with newDir. The first time newDir direction is observed → that's the reversal point. That seems correct.

Let me look at this from a different angle. What if the problem is simpler — what if fetchTideRecords is called but updateTide is called with an empty object? Let me check if there's a .catch that swallows errors silently.

In usePumpData.ts:103-105:
fetchTideRecords(hoursBack).then(tideRecords => {
  if (mountedRef.current) updateTide(tideRecords);
}).catch(() => {});

The .catch(() => {}) silently swallows all errors. If fetchTideRecords throws, nothing happens and no logs get created.

I think the issue might be in the fetchTideRecords function. Let me look more carefully. The results array order matters:

Line 271-276: results is from 6 parallel requests: 112主, 112備, 110主, 110備, 108主, 108備.

Line 282-283: for i=0..2, no = TIDE_STATIONS[i], primary = results[i*2], backup = results[i*2+1].

That looks correct.

But wait, there's a subtle issue with addRecords at line 289-296:
const addRecords = (list: RawStationData[] | null) => {
  if (!list) return;
  for (const r of list) {
    const key = r.rectime;
    if (!map.has(key) || r.rectime > (map.get(key)!.rectime)) {

This adds records to the map keyed by rectime. But both primary and backup may return the same data, and this correctly takes the entry with the newer rectime (though since they have same key, this comparison is almost always about ties).

Hmm wait, I think I found it. The fetchRawTide can return null if the response is null. Line 221 and 246 both do return json.d || null. If the API returns {"d": [...]}, it returns the array. But if the API returns {"d": null} or {"d": []}, it returns null OR [].

When addRecords gets null, it skips. When it gets [], it loops over nothing and the map stays empty. That's fine.

But what about the case where fetchPost on Android returns null? In the Kotlin/Java bridge, return null is a String. In JavaScript, this becomes "null" (string) or null (JS null)?

Actually, the bridge returns a Java String to JavaScript. When Java returns null, JavaScript gets null. So the jsonStr check at line 219 should correctly handle this.

Let me take a step back and check if the actual issue is simpler — maybe fetchTideRecords returns an empty array because the API is called with the wrong station numbers or time range. The shinshun project uses station 112 specifically. Let me check the TIDE_STATIONS.

  Searched for 1 pattern, read 1 file

OK, TIDE_STATIONS are ['108', '110', '112']. These match the station IDs from the main API.

Now let me look at this from the user's report one more time: "app啟動後潮汐紀錄不會補上" — the tide records don't replenish after app startup. Let me check if mountedRef is the issue — when the app starts from a fresh launch, does the fetchData callback get called at all?

Looking at usePumpData.ts:96-105:
// 潮汐檢查（背景非同步，不阻塞首頁載入） — 每次輪詢都執行，去掉 lastTideCheckTime 限制
console.log('[Tide Debug] fetchData: about to fetch tide records');
const state = useStore.getState();
const hasPrevDir = Object.keys(state.tideDirection).length > 0;
const hoursBack = hasPrevDir ? 3 : 12;
fetchTideRecords(hoursBack).then(tideRecords => {
  if (mountedRef.current) updateTide(tideRecords);
}).catch(() => {});

The fetchTideRecords runs, but the .then() runs updateTide only if mountedRef.current is true. That should be fine.

OK

  Read 1 file

I think I may have found it. Let me look at the type of level_out coming from the RawStationData. In RawStationData, level_out is number | null. In TideRecord, level_out is number | null. That should be fine.

Wait — let me re-examine the detectOnSlice function and how it handles the initial cold scan:

const detectOnSlice = (records: TideRecord[]): TideDirection | null => {
  const valid = records.map(r => r.level_out).filter(v => v !== null) as number[];
  if (valid.length < 2) return null;
  let inc = 0, dec = 0;
  for (let i = 1; i < valid.length; i++) {
    if (valid[i] > valid[i - 1]) inc++;
    else if (valid[i] < valid[i - 1]) dec++;
  }
  if (dec > inc) return 'falling';
  if (inc > dec) return 'rising';
  return 'slack';
};

This filters out nulls from the 5-record window. If 4 or more records have null level_out, it returns null. But if 2-5 have valid values, it returns a direction.

If all valid pairs are equal (inc=0, dec=0), it returns 'slack'. This is important — it means even if values are all flat, it successfully returns a direction.

Now in the cold scan:
- When lastDir is null and a new dir is found (which could be 'slack'), lastDir = 'slack'
- Next iteration, if dir is still 'slack', no log is pushed
- If it changes to 'rising' or 'falling', a log IS pushed

But there's a key edge case I missed: what if the FIRST window returns 'slack' (all values flat), and the SAME result persists for ALL 12 hours of data? Then logCount stays 0, and nothing gets logged. That's possible but unlikely for 12 hours of data.

Actually, let me re-examine more carefully. Let me trace through the logic:

1. records has entries sorted by rectime ascending (oldest first)
2. i = 5, slice = records[0..4], first 5 records
3. If detectOnSlice(slice) returns null (not enough valid data), continue — no log, no lastDir update
4. If it returns 'slack', 'rising', or 'falling': lastDir is null, so nothing to compare. Sets lastDir = dir, then i++
5. i = 6, slice = records[1..5], 5 records shifted by 1
6. Since lastDir is now set, if dir !== lastDir, push a log entry

Wait, I think I see the actual issue now. Let me check what happens with the detectOnSlice for a stale level_out value. If level_out for a tide station hasn't been updated in the API for hours, all records would have the same level_out value (or null). In that case:
- If null → valid.length < 2 → returns null → skipped
- If all same value → inc=0, dec=0 → returns 'slack'

So the cold scan would produce lastDir = 'slack' for every 5-record window. Since every window returns 'slack', logCount = 0. No logs are created!

But wait, there's more. The newDirections at line 69-70 shows:
newDirections['112'] = detectTide(tideRecords['112'], '112');

And detectTide:
const valid = records.slice(-5).map(r => r.level_out).filter(v => v !== null) as number[];
if (valid.length < 2) { console.warn(...); return prevDirections[stationNo] ?? 'slack'; }

If there are no valid entries, it returns the previous direction or 'slack'. On cold start, previous is undefined, so it returns 'slack'.

So newDirections['112'] = 'slack' in most cases. Then at line 130:
const prevDir = prevDirections[stationNo];  // undefined on cold start
if (prevDir) { ... }  // skipped on cold start
else { /* cold start path */ }

The cold start path runs. But detectOnSlice returns 'slack' for most windows (if data is flat), and there's no reversal to log. The scan runs to completion with logCount = 0.

This is the bug: if all the level_out values are flat (or null), the cold start produces no tide log entries at all. The user sees a blank tide log because the cold scan can't find any reversals.

BUT — there's another possibility. What if there ARE direction changes, but the window size of 5 is too small to catch them if the change happened halfway through a window? No, with 5-record windows sliding by 1, it should catch any change that lasts at least 2 data points.

Let me look at the problem from a different angle. Looking at the fetchTideRecords function again more carefully.

Actually, I now realize there might be a simpler bug. Let me look at line 282 in pumpStation.ts more carefully:

for (let i = 0; i < TIDE_STATIONS.length; i++) {
    const no = TIDE_STATIONS[i];
    const primary = results[i * 2];
    const backup = results[i * 2 + 1];

results has 6 elements. TIDE_STATIONS has 3 elements. i=0 → no='108', primary=results[0], backup=results[1].

Wait, TIDE_STATIONS = ['108', '110', '112']. The flatMap on line 272 generates:
- 108主, 108備, 110主, 110備, 112主, 112備

So results[0] = 108 primary, results[1] = 108 backup, results[2] = 110 primary, etc. That's correct.

OK, I think I should focus on the actual cold start issue. Let me think about what "潮汐紀錄" the user expects to see. From the OperationLogs.tsx, the tide tab shows direction changes (e.g., rising → falling). If the cold start doesn't detect any direction reversals in the 12-hour scan, nothing shows. And even if the warm path catches a change, it only catches it from the current scan.

The fundamental issue is: on cold start, prevDirections is empty, so updateTide enters the cold start path. But the cold start path's sliding window scan can fail to produce logs if data has few valid points or no direction changes in the window. Additionally, the warm path (subsequent polls) only adds a log when direction changes, not for the initial direction determination.

Here's my diagnosis of the two bugs:

Bug 1: The cold start scan stores the last detected direction via lastDir at the end of the loop, but this direction is not persisted anywhere usable — newDirections was already computed by detectTide before the scan runs. The scan only produces history entries about reversals but doesn't contribute to the current direction. The detectTide (line 68-74) uses only the last 5 records and the previous direction. On cold start, previous is undefined, so new direction defaults to last 5 records dir or 'slack'.

Bug 2: The cold start scan's logCount stays 0 if:
- data has too many nulls (returns null from getOnSlice → skipped)
- data is all flat (remains 'slack' the whole scan → no reversals logged)

These edge cases make the tide log empty.

But actually, I think the most likely issue is even simpler: the tide records from GetAutoPumpWaterMins API might not have any level_out values at all, or very few. Let me verify by checking the shinshun reference.

  Read 1 file, recalled 1 memory, searched memories

Good, the API reference confirms the format. Now I have a clear picture. Let me identify the root cause precisely.

The issue is in updateTide in tideSlice.ts. On app cold start:

1. fetchTideRecords(12) is called — this fetches 12 hours of data via GetAutoPumpWaterMins API
2. updateTide(tideRecords) is called with the results
3. detectTide (line 68-74) computes newDirections using only the last 5 records
4. The cold start scan (line 135-162) slides a 5-record window through ALL records
5. If there are direction reversals in the historical data, they get logged to tideOperationLog
6. But if the scan finds NO reversals (all flat data), nothing is logged

The problem is**: the cold start scan's lastDir variable is scoped to the outer loop (line 143: let lastDir: TideDirection | null = null) and set within the scan loop. After the scan completes, lastDir has a valid direction from the scan — but this direction is discarded. It's not stored anywhere.

Meanwhile, newDirections was computed independently by detectTide using only 5 last records. But newDirections never gets reflected as a "tide log entry" because the cold start path only creates entries for REVERSALS, not for the initial state.

So the fix should be: after the cold start scan, record the initial direction as a log entry if no reversals were found. This way, even if the direction didn't change in the past 12 hours, the UI shows the current tide state.

Let me implement this fix:

● Update(src\store\slices\tideSlice.ts)
Added 6 lines, removed 1 line
          lastDir = dir;
        }
        console.log(`[Tide Debug] ${stationNo}: cold scan done, found ${logCo
unt} reversals, lastDir=${lastDir}`);
      }
          // 如果沒有找到任何轉折，至少記錄初始潮汐狀態
          if (logCount === 0 && lastDir && lastDir !== 'slack') {
            const firstTime = rectimeToMs(records[0].rectime);
            newTideLog.push({ timestamp: firstTime, stationNo, from: 'rising'
 === lastDir ? 'falling' : 'rising', to: lastDir });
          }
        }
    }

    if (newTideLog.length > 0) {

Wait, that approach is a bit fragile — using 'rising' === lastDir ? 'falling' : 'rising' as the "from" direction feels wrong. Let me reconsider.

Actually, the better approach is different. The newDirections was already computed before the scan. On cold start, even if no reversal is found, we should record the current direction as an initial log entry. The scan already tracks the lastDir. But what we really want is to record newDirections[stationNo] as the initial entry.

Let me revise:

● Update(src\store\slices\tideSlice.ts)
Added 2 lines, removed 2 lines
        }
        console.log(`[Tide Debug] ${stationNo}: cold scan done, found ${logCo
unt} reversals, lastDir=${lastDir}`);
          // 如果沒有找到任何轉折，至少記錄初始潮汐狀態
          if (logCount === 0 && lastDir && lastDir !== 'slack') {
          if (logCount === 0 && newDir && newDir !== 'slack') {
            const firstTime = rectimeToMs(records[0].rectime);
            newTideLog.push({ timestamp: firstTime, stationNo, from: 'rising'
 === lastDir ? 'falling' : 'rising', to: lastDir });
            newTideLog.push({ timestamp: firstTime, stationNo, from: 'slack',
 to: newDir });
          }
        }
    }

Now, this fix alone won't completely solve the problem because there's a bigger issue: the newDir is the direction from the last 5 records, but detectTide uses records.slice(-5) which may not have enough valid data. Let me also check if the warm path (non-cold-start) properly logs entries on first direction recognition.

Actually, let me step back and think about the user's specific complaint: "潮汐紀錄不會補上". This means the tide log tab is EMPTY. The tide log tab shows tideOperationLog entries which are direction changes. Let me trace through what happens during a typical cold start:

1. App launches, usePumpData hook mounts
2. fetchData() is called
3. Phase A: main API + alarm check (fast)
4. Phase B: fetchTideRecords(12) is called (12 hours because no previous direction)
5. updateTide(tideRecords) is called

In updateTide:
- detectTide computes newDirections for 112 and 108
- Cold start scan for EACH tide station
- Scan may or may not find reversals

Let me now also think about the other problem I noticed: detectTide at line 53 ONLY uses the last 5 records. If none of those records have valid level_out, it returns prevDirections[stationNo] ?? 'slack'. On cold start, prevDirections['112'] is undefined, so it returns 'slack'.

Then at line 122:
for (const stationNo of TIDE_STATIONS) {
  const records = tideRecords[stationNo];
  if (!records || records.length < 3) continue;

If records.length < 3, the entire processing (cold start scan + log generation) is SKIPPED. This is a second bug: the threshold of 3 minimum records at line 125 is too strict for cold start — maybe the API only returns a few records.

But wait, the API should be returning 12 hours of data (~144 records). So records.length should be large enough.

Unless... the GetAutoPumpWaterMins API for stations 108 and 110 returns different/less data than for 112 (the main station). The shinshun project only uses station 112.

OK, I think I've been overthinking this. Let me look at this from a simple perspective: the user says "records don't show up". The most likely cause is that the API returns records but the scan doesn't produce log entries. My fix above adds the initial entry. But let me also make sure the scan is actually running — that the records array has enough data (≥3 records).

Let me verify my changes look correct by reading the modified area.

● Read(d:\cc project\水位即機組監控警示系統\src\store\slices\tideSlice.ts · lines 120-189)
Read 70 lines