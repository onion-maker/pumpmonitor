# 目前進度

## 版本：v1.7.4

### ✅ 已完成

#### T15 — 抽水機組運轉紀錄
- `src/components/OperationLogs.tsx` — 操作紀錄組件
  - 抽水機紀錄 / 閘門紀錄 雙標籤切換
  - 站點下拉篩選（全部站點 or 單一站點）
  - 表格顯示：時間、站點、設備編號、動作
  - 清除紀錄按鈕（含確認對話框）
- 整合於 `SettingsPage.tsx` 設定頁面底部

#### T16 — 閘門啟閉紀錄
- 與 T15 共用 `OperationLogs.tsx`，切換標籤即可查看

#### T17 — 卡片內水位曲線圖
- `src/components/WaterLevelChart.tsx` — 小型水位折線圖
  - 使用 **recharts** (LineChart, Line, Tooltip, ReferenceLine)
  - 內池水位（藍色 #3b82f6） + 外池水位（綠色 #10b981）
  - 警戒水位紅色虛線（ReferenceLine）
  - Y 軸自動範圍
  - Tooltip 顯示數值 + m 單位（`as any` 繞過 recharts 型別不相容）
  - dark mode 支援
- 整合於 `StationCard.tsx`（卡片內水位條下方）

#### 其他修復
- `settingsSlice.ts` — 補上 `wakeLockMode` 預設值 `'low'` 和 `setWakeLockMode` setter
- `alarmSlice.ts` — alarm 邏輯更新
- `pumpStation.ts` — API 層微調

### 📁 本次變更檔案

| 檔案 | 變更 |
|------|------|
| `src/components/OperationLogs.tsx` | 新增 |
| `src/components/WaterLevelChart.tsx` | 新增 |
| `src/components/StationCard.tsx` | import + 插入圖表區塊 |
| `src/pages/SettingsPage.tsx` | import + 插入 OperationLogs |
| `src/store/slices/settingsSlice.ts` | wakeLockMode 預設值 + setter |
| `src/store/slices/alarmSlice.ts` | alarm 邏輯更新 |
| `src/api/pumpStation.ts` | API 層微調 |
| `package.json` / `package-lock.json` | 添加 recharts 依賴 |
| `設計工作紀錄.md` | T15/T16/T17 設計決策記錄 |

### 🔜 待辦

- [ ] 測試潮汐警報邏輯確認正常
- [ ] 水位曲線圖目前顯示單點，未來可考慮接入 `GetAutoPumpWaterMins` 顯示歷史曲線
- [ ] 如需發佈 APK，執行 `npm run build` 後同步 Android 專案

### ⚠ 已知問題

- `package.json` / `package-lock.json` 的 recharts 依賴尚未 commit（因 build 後 git checkout 復原了這兩個檔案，需下次補 commit）

---
有兩個方向可以改善：

1. 直接拉高圖表：把預設 height 從 50px 調到 100~120px，簡單直接
2. 點擊彈出大圖：保留小圖預覽，點擊後 modal/展開顯示大尺寸圖表（~250px），更能看清楚細節

你想要哪個方案？還是兩個都做（拉高預設 + 點擊彈大圖）？

✻ Brewed for 27s

❯ 兩個都做，另外點擊圖表時發現內池水位的文字也標示成了外池

讓我先確認一下 Tooltip 的 bug，再看目前 StationCard 的 onChartClick 有沒有綁定。

  Read 2 files

看到兩個問題：

1. Tooltip bug：formatter 的 name 參數在 Recharts 是 dataKey 而不是 name prop。Line 的 name="內池" 跟 name="外池" 在 formatter 收到的 name 可能是 "level_in" / "level_out"（dataKey），要看 Recharts 版本行為。我改用 entry.name（或直接判斷 dataKey）。
2. onChartClick 沒綁 — StationCard 沒傳 onChartClick，需要加 modal 展開大圖。

開始實作。

Tooltip 的 formatter callback 在 recharts 中，name 參數是 Line 的 name prop（不是 dataKey）。但實際測試發現有些 recharts 版本給的是 dataKey。最穩的方法是改用 formatter 的第三個參數（payload），或直接用 Tooltip 的 itemFormatter。不過最簡單的修法：判斷 value 對應哪個 dataKey。

其實 Recharts formatter(value, name, props) — name 應該就是 Line 的 name prop。但如果出現 bug 說「內池標示成外池」，那可能是 Line 的 name prop 沒生效。我直接用 Tooltip callback 的 payload 來判斷：

● Update(src\components\WaterLevelChart.tsx)
Added 5 lines, removed 3 lines
            axisLine={false}
          />
          <Tooltip
            labelFormatter={(label) => `時間: ${label}`}
            contentStyle={{ fontSize: '10px', padding: '4px' }}
            formatter={((value: any, name: any) => {
              const numValue = Number(value);
              if (isNaN(numValue)) return ['--', name];
              return [`${numValue.toFixed(2)}m`, name === 'level_in' ? '內池' : '外池'];
              // name 可能是 dataKey (level_in/level_out) 或 Line 的 name prop (內池/外池)
              const label = name === 'level_in' || name === '內池' ? '內池' : '外池';
              return [`${numValue.toFixed(2)}m`, label];
            }) as any}
            labelFormatter={(label) => `時間: ${label}`}
            contentStyle={{ fontSize: '10px', padding: '4px' }}
          />
          {alarmLevel !== undefined && (
            <ReferenceLine


*更新日期：2026-07-24*