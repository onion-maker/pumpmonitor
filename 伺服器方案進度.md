完成！server 端檔案結構

pumpmonitor-server/
├── package.json
├── tsconfig.json
└── src/
    ├── config.ts         # 設定檔
    ├── types.ts          # TypeScript 型別
    ├── api.ts            # API 呼叫 (fetchAllStations, fetchTideRecords)
    ├── algorithms.ts     # 演算法 (tide detection + alarm logic)
    ├── fcm.ts            # Firebase Cloud Messaging
    └── index.ts          # Express 伺服器 + 輪詢

---
下一步：設定 Firebase

您需要在 Firebase Console 設定：

1. 服務帳號金鑰 (Service Account):
  - Firebase 控制台 → 專案設定 → 服務帳號 → 產生新的私鑰
  - 下載 serviceAccountKey.json
  - 轉成 base64：cat serviceAccountKey.json | base64 -w0
  - 加入環境變數：FIREBASE_SERVICE_ACCOUNT_BASE64=...
2. FCM Token 管理:
  - 前端 App 需要呼叫 /api/register-fcm 註冊 token
  - Android App 可以在 MainActivity.java 加入：
// 一次登入後註冊 token
FirebaseMessaging.getInstance().getToken().addOnCompleteListener(task -> {
  if (task.isSuccessful()) {
    String token = task.getResult();
    // POST 到 server /api/register-fcm
  }
});

---
下一步行動建議

按您的喜好，你可以：

A. 本地測試 Server

cd "/d/cc project/pumpmonitor-server"
npm install
npm run dev

B. 部署到 GCP

# 需要設定環境變數
export FIREBASE_SERVICE_ACCOUNT_BASE64="..."
export HEOV_API_URL="https://heovcenter.gov.taipei/cia/WebLayout/GetLastestAutoPumpPGInfo"
# ...
npm run build
npm start

C. 修改前端

1. 把 PumpMonitorService.java 從後端輪詢改成只收 FCM
2. 把 MainActivity.java 保留 FCM 註冊邏輯
3. 把 React 前端的 usePumpData 改成從 server 拉資料

---
**您要先做哪一步？**我可以幫您：
- 幫您把 Android App 端的 FCM 註冊碼寫好
- 幫您把 React 前端改成呼叫 server API
- 把 Express server 部署到 GCP 的 Dockerfile 寫好

完整操作指南

需做的事 (在 Android Studio):

1. 建立 PumpAlarmReceiver.java (裝置: android/app/src/main/java/com/pumpmonitor/app/):
package com.pumpmonitor.app;
// + 整段 Java 碼 (上面給的)
2. 修改 MainActivity.java : 加入 Firebase Messaging 註冊
3. 修改 AndroidManifest.xml :
<service
    android:name=".PumpAlarmReceiver"
    android:exported="false">
    <intent-filter>
        <action android:name="com.google.firebase.MESSAGING_EVENT" />
    </intent-filter>
</service>
4. 確認 build.gradle 已加入:
dependencies {
    implementation 'com.google.firebase:firebase-messaging:23.0.0'
}

---
請問：
您想要 手動操作，還是 我再試一次寫檔案 (看看能不能成功)？你


您現在要在 Android Studio 裡把 FCM 設定補完：

---
🛠️ 您要做的 4件事:

1. 編輯 AndroidManifest.xml：
android/app/src/main/AndroidManifest.xml
1. 加一行：
<service android:name=".PumpAlarmReceiver" android:exported="false">
    <intent-filter>
        <action android:name="com.google.firebase.MESSAGING_EVENT" />
    </intent-filter>
</service>
2. 編輯 build.gradle (app)：
android/app/build.gradle
2. 加一行：
implementation 'com.google.firebase:firebase-messaging:23.0.0'
3. 放 google-services.json：
  - 下載自 Firebase Console
  - 放到 android/app/google-services.json
4. 編輯 MainActivity.java：
  - 加 FCM 註冊碼

---
如果不懂，請您先：

打開 Android Studio → Project → app/src/main → 依序編輯上面這些檔案

---
結果：

完成之後：
- Android App 收得到伺服器的 FCM 推播
- 您可以看到 手機收到警報通知
- App 不需要後台服務了

---
我下一步幹什麼？

等您完成 Android 設定後，我會 寫 React 前端 FCM 端，然後：

完整流程:
Server API Polling → 判斷警報 → FCM Push → 手機收到

---
您先堵一個Android，這邊等您~
😊
目前 您要做的：

Android 4 步驟：

1. AndroidManifest.xml → 加入 FCM service tag
2. build.gradle → 加入 firebase-messaging
3. google-services.json → 放到 android/app/
4. MainActivity.java → 加入 token 註冊

---
請您回復：

"已完成 X 步驟"，或 "想跳過, 我們先做 server"

我會根據您的選擇繼續推進：

- 如果 Android 完成 → 我們立刻寫 React FCM 端
- 如果 您要跳過 → 我們先把 React FCM 端寫好，您有時間再回來補 Android 端
目前 4 步驟狀態：

┌─────────────────────────────────────────────────────┬─────────────────┐
│                        步驟                         │      狀態       │
├─────────────────────────────────────────────────────┼─────────────────┤
│ 1. AndroidManifest.xml → FCM service                │ ✅              │
├─────────────────────────────────────────────────────┼─────────────────┤
│ 2. build.gradle → firebase-messaging 依賴           │ ✅ 幫我確認一下 │
├─────────────────────────────────────────────────────┼─────────────────┤
│ 3. google-services.json → android/app/              │ ✅              │
├─────────────────────────────────────────────────────┼─────────────────┤
│ 4. MainActivity.java → initFcm() 在 onCreate 中呼叫 │ ✅              │
└─────────────────────────────────────────────────────┴─────────────────┘

---
build.gradle 的 firebase-messaging 依賴加了嗎？ 幫我確認一下，不然編譯會失敗。

📋 現在可以做的事

⚡ 立即測試

# 生產前 build APK
cd "D:/cc project/水位即機組監控警示系統"
npx cap sync android
cd android && ./gradlew assembleRelease
- 登入後會自動註冊 FCM token
- 資料透過 Cloud Run server 取得 (fetchStationsFromServer())

🔐 加入 Firebase 金鑰 (讓 FCM 推播生效)

# 把 serviceAccountKey.json 壓成一行 base64
cd "d:/cc project/水位即機組監控警示系統"
$base = [Convert]::ToBase64String([IO.File]::ReadAllBytes("android/app/serviceAccountKey.json"))
$base | Set-Clipboard

# 然後：
# GCP Console → Cloud Run → pumpmonitor-server → Edit → Variables & Secrets → 加一行：
# FIREBASE_SERVICE_ACCOUNT_BASE64 = <貼上 Ctrl+V>