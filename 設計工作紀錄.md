# 設計工作紀錄

## 2025-07-23 T15-T16: 操作紀錄顯示功能

### 需求描述
- **T15**：抽水機組的運轉紀錄顯示
- **T16**：閘門的啟閉紀錄顯示

### 調查結果

#### 後端實作狀況
1. **Store (alarmSlice.ts)**
   - ✓ pumpOperationLog 狀態陣列
   - ✓ gateOperationLog 狀態陣列
   - ✓ addPumpOperationLog() 方法
   - ✓ addGateOperationLog() 方法
   - ✓ getPumpLogsByStation(stationNo) 方法
   - ✓ getGateLogsByStation(stationNo) 方法
   - ✓ clearOperationLogs() 方法

2. **Types (types/index.ts)**
   - ✓ PumpOperationLogEntry 型別定義
   - ✓ GateOperationLogEntry 型別定義

3. **Android 橋接**
   - ✓ getOperationLogs() - 取得所有紀錄
   - ✓ clearLogs() - 清除所有紀錄
   - ✓ logPumpOperation() - 記錄抽水機操作
   - ✓ logGateOperation() - 記錄閘門操作

4. **工具函數 (backgroundAlarm.ts)**
   - ✓ getOperationLogs() - 前端調用橋接
   - ✓ clearOperationLogs() - 前端調用橋削

#### 前端缺失
- ❌ 無操作紀錄顯示的 UI 組件
- ❌ 無在 SettingsPage 中顯示紀錄的區域

### 設計決策

#### 顯示位置
選擇在 **SettingsPage** 中添加操作紀錄區域

**決定理由：**
1. 操作紀錄是較量的資訊，不需要在主畫面即時顯示
2. 設定頁面已經有類似的功能區塊
3. 易於實作和維護

#### UI 結構
```
SettingsPage
├── 基礎設定區域
└── 操作紀錄區域 (新增)
    ├── 站點篩選下拉選單
    ├── 紀錄類型切換選卡
    │   ├── 抽水機紀錄
    │   └── 閘門紀錄
    ├── 紀錄表格
    └── 清除紀錄按鈕
```

## 實作完成狀況

### 完成的工作
1. ✅ 在 SettingsPage.tsx 中添加了 OperationLogs 組件的 import
2. ✅ 在 SettingsPage.tsx 中添加了 OperationLogs 組件的顯示位置
3. ✅ 創建了 OperationLogs.tsx 組件
   - 支持抽水機紀錄和閘門紀錄的切換
   - 支持按站點篩選
   - 提供清除紀錄功能
   - 表格展示時間、站點、設備編號、動作
4. ✅ 修復了 settingsSlice.ts 中的缺失屬性 (wakeLockMode, setWakeLockMode)
5. ✅ 應用成功編譯

### 文件變更
- `src/components/OperationLogs.tsx` - 新增檔案
- `src/pages/SettingsPage.tsx` - 修改（添加import和組件）
- `src/store/slices/settingsSlice.ts` - 修改（添加缺失屬性）

### 測試結果
- ✅ TypeScript 編譯成功
- ✅ Vite 編譯成功
- ✅ 生成的 JS 檔案無明顯錯誤

## T17: 卡片內水位曲線圖 功能設計

### 需求概述
在每個站點卡片內添加小型水位曲線圖，顯示近2小時的內外水位變化趨勢。點擊後可放大查看詳細圖表。

### 用戶需求
1. ✅ 卡片內顯示圖表
2. ✅ 小尺寸格式 (數據變化就能看見)
3. ✅ 可點擊放大
4. ✅ 使用圖表庫
5. ✅ 顯示內池水位、外池水位

### 設計架構

```
StationCard
├── 基本資訊區塊
│   ├── 站名、時間、警報
│   └── 警報水位輸入
├── 水位圖表區塊 (新增)
│   ├── 小尺寸折線圖
│   │   ├── 內池水位 (blue)
│   │   ├── 外池水位 (green)
│   │   └── 警戒水位線 (red dashed)
│   └── 點擊放大彈出
└── 剩餘區塊 (PumpGrid, DoorGrid...)
```

### 技術方案

#### 圖表庫選擇
**推薦：Recharts** (React原生圖表庫)
- ✅ 與 React 完美整合
- ✅ 樣式易於控制
- ✅ 支持 responsive 佈局
- ✅ 檔案大小小

#### 資料來源
- 從 `tideSlice.ts` 的 `tideBuffer` 取得
- 或使用現有的 `FetchSiteData` API

#### 組件結構
1. **WaterLevelChart.tsx** - 小尺寸折線圖組件
   - 接收資料陣列作為 prop
   - 使用 ResponsiveContainer 包裹
   - 返回 SVG 圖表

2. **StationCard.tsx** - 修改後
   - 導入 WaterLevelChart
   - 添加圖表區塊
   - 點擊事件觸發放大

3. **waterLevelService.ts** - 工具函數
   - `getLastHourWaterLevel(stationNo: string)` - 取得最近1小時資料
   - `formatChartData(records: [])` - 格式化資料給圖表使用

#### 圖表規格
- X軸：時間 (HH:mm 格式)
- Y軸：水位值 (m)
- 線條：
  - 內池：blue (#3b82f6)
  - 外池：green (#10b981)
  - 警戒水位：red dashed

#### 顏色主題
```css
/* 深色模式 */
.graph-area { background: #1f2937; }
.graph-axis { color: #9ca3af; }
.graph-grid { stroke: #374151; }
```

### 實作結果（2026-07-24 完成）

#### 已完成工作

1. ✅ `npm install recharts` — 安裝圖表依賴
2. ✅ `src/components/WaterLevelChart.tsx` — 建立小型水位折線圖組件
   - LineChart with XAxis (hide), YAxis (domain auto-range)
   - Two Lines: level_in (blue #3b82f6), level_out (green #10b981)
   - ReferenceLine for alarm threshold (red dashed)
   - Tooltip formatter with `as any` to bypass recharts Formatter type incompatibility
   - ResponsiveContainer wrapping for mobile responsiveness
   - Dark mode border styling
3. ✅ `StationCard.tsx` — 整合 WaterLevelChart 於卡片內
   - Import at line 7
   - Component rendered around line 251, passing level_in/level_out/alarmLevel props
4. ❌ `src/utils/waterLevelService.ts` — 建立後刪除（TideRecord 型別不存在於 types/index.ts，且目前僅顯示單點資料，不需要歷史曲線服務）

#### TypeScript 型別問題處理

recharts 的 `Tooltip` `formatter` prop 型別為 `Formatter<ValueType, NameType>`，其中 `value` 是 `ValueType | undefined`、`name` 是 `NameType | undefined`。我們的 formatter 參數型別與之不相容。

**解決方案**: 使用 `(value: any, name: any) => [...]` 參數型別 + 整個函式後綴 `as any` 雙重繞過。

```tsx
formatter={((value: any, name: any) => {
  const numValue = Number(value);
  if (isNaN(numValue)) return ['--', name];
  return [`${numValue.toFixed(2)}m`, name === 'level_in' ? '內池' : '外池'];
}) as any}
```

#### 編碼問題處理

bash heredoc 寫入含中文字元的 .tsx 檔案會導致編碼損壞（反引號轉義成 `\``）。此檔案改用 Edit 工具修正。

#### 檔案變更摘要

| 檔案 | 變更 |
|------|------|
| `src/components/WaterLevelChart.tsx` | 新增 |
| `src/components/StationCard.tsx` | import + 插入圖表區塊 |
| `package.json` + `node_modules/` | 添加 recharts 依賴 |
| `src/utils/waterLevelService.ts` | 建立後刪除（型別不存在，暫不需要） |

### 相關檔案
- `src/components/WaterLevelChart.tsx`
- `src/components/StationCard.tsx` (lines 7, 251)

---

審計完成了，共找出 15 個問題（6 高、7 中、2 低）。以下是整理結果：

---
高嚴重度 (6)

┌─────┬────────────────────────────────────────────────────────┬─────────────────────────┐
│  #  │                          問題                          │          位置           │
├─────┼────────────────────────────────────────────────────────┼─────────────────────────┤
│ 3   │ Firebase eager import — main.tsx 頂層 import           │ main.tsx:3              │
│     │ firebase/app，阻斷 tree-shaking，bundle 達 681KB       │                         │
├─────┼────────────────────────────────────────────────────────┼─────────────────────────┤
│     │ 全量 store.subscribe — App.tsx + useStore.ts 兩個      │ App.tsx:150,            │
│ 4   │ subscribe 都無 selector，每次 store 任何變化都觸發     │ useStore.ts:34          │
│     │ JSON.stringify diff，30 秒一次                         │                         │
├─────┼────────────────────────────────────────────────────────┼─────────────────────────┤
│ 5   │ StationCard 12 次 selector — 每卡 12 次 useStore(s =>  │ StationCard.tsx         │
│     │ s.xxx)，15 站 × 30 秒 = 大量 re-render                 │                         │
├─────┼────────────────────────────────────────────────────────┼─────────────────────────┤
│ 2   │ 339KB MP3 隨 bundle — public/                          │ config/stations.ts:49   │
│     │ 目錄的警報音檔，首次載入就被 preload，阻塞渲染         │                         │
├─────┼────────────────────────────────────────────────────────┼─────────────────────────┤
│ 6   │ Audio memory leak — playStationAlarm 每次 new Audio +  │ audio.ts:20-29,         │
│     │ loop，base64 塞 localStorage 有大小風險                │ tideSlice.ts:141        │
└─────┴────────────────────────────────────────────────────────┴─────────────────────────┘

中嚴重度 (7)

┌─────┬──────────────────────────────────────────────────────────────────────────────┐
│  #  │                                     問題                                     │
├─────┼──────────────────────────────────────────────────────────────────────────────┤
│ 8   │ fetchWaterLevelHistory 平行對 15 站發 30 個 HTTP request（應只針對選取站點） │
├─────┼──────────────────────────────────────────────────────────────────────────────┤
│ 9   │ lastUpdateTime 存 locale string 又反向 parse，來回浪費                       │
├─────┼──────────────────────────────────────────────────────────────────────────────┤
│ 10  │ Header 每秒 clock setInterval，背景也繼續跑                                  │
├─────┼──────────────────────────────────────────────────────────────────────────────┤
│ 7   │ AlarmBanner/StationCard 用 array index 作 key                                │
├─────┼──────────────────────────────────────────────────────────────────────────────┤
│ 11  │ LoginPage 和 useAuth 重複 biometric 程式碼 + callback leak                   │
├─────┼──────────────────────────────────────────────────────────────────────────────┤
│ 12  │ pumpOperationLog/gateOperationLog 無上限存 localStorage                      │
├─────┼──────────────────────────────────────────────────────────────────────────────┤
│ 13  │ syncSettingsToNative 雙重 JSON.stringify                                     │
├─────┼──────────────────────────────────────────────────────────────────────────────┤
│ 1   │ index.html 缺 PWA manifest / meta tags                                       │
└─────┴──────────────────────────────────────────────────────────────────────────────┘

低嚴重度 (2)

┌─────┬───────────────────────────────────────────────────────┐
│  #  │                         問題                          │
├─────┼───────────────────────────────────────────────────────┤
│ 14  │ animate-scaleIn 缺少 @keyframes 定義                  │
├─────┼───────────────────────────────────────────────────────┤
│ 15  │ tideSlice 用 stationData.find 取站名，可直接用 config │
└─────┴───────────────────────────────────────────────────────┘
高嚴重度

#3 Firebase eager import (main.tsx:3)

import { initializeApp } from 'firebase/app';

這行在 bundle entry 頂層，Rollup/Vite 無法 tree-shake。Firebase JS SDK v11 的 firebase/app 模組整包 ~90KB gzipped。即使其他子模組（auth、firestore）是用 lazy await import() 正確處理，但 entry 層這個 eager import 讓整個 app 模組直接打入主 chunk。目前 dist/assets/index-*.js 681KB，其中 firebase/app 佔很大比例。

影響: 首次載入慢。Android WebView 每次啟動都要 parse + compile 這 681KB JS，尤其是在舊手機上可能多花 1-2 秒。

修法: 把 initializeApp 也改 lazy，跟 auth 初始化放一起。

---
#4 全量 store.subscribe (App.tsx:150, useStore.ts:34)

// App.tsx — 設定變更時同步到 Native
const unsub = useStore.subscribe(sync);

// useStore.ts — localStorage 儲存
useStore.subscribe((state) => { ... });

Zustand subscribe() 不帶 selector 參數時，store 內任何一個欄位變化都會觸發 callback。每 30 秒輪詢更新 stationData、lastUpdateTime 等，全部觸發這兩個 subscribe：

- App.tsx 的 sync callback 內做 JSON.stringify 整包設定 snapshot 比對是否變更，然後 call native bridge
- useStore.ts 的 save callback 內做 JSON.stringify 整包設定寫 localStorage（還有 500ms debounce）

影響: CPU 浪費。每 30 秒兩次 JSON.stringify 遍歷整個 store（包含 stationData 15 站完整資料），加上 Android WebView JS engine 效能不如桌面 Chrome，可能造成 UI 卡頓。電池消耗也增加。

修法: 改用 subscribe(selector, callback) 只監聽真正要 sync 的那幾個設定欄位。

---
#5 StationCard 12 次 selector

const stationAlarmLevels = useStore((s) => s.stationAlarmLevels);
const setStationAlarmLevel = useStore((s) => s.setStationAlarmLevel);
const alarmingStations = useStore((s) => s.alarmingStations);
// ... 共 12 行

每次 store 任何欄位變化（例如 lastUpdateTime），這 15 張卡 × 12 selector = 180 次 selector 函數執行。alarmingStations 是陣列，tideDirection 是物件，Zustand 預設用 Object.is（shallow），每次 store set 回傳新 reference，一定觸發 re-render。即使該站沒警報、tide 沒變化，也會重新 render。

影響: 每 30 秒全站卡片集體 re-render 一次，15 張卡同時更新會讓 UI 掉幀。

修法:
- 多個純取值 selector 合併用 useShallow（Zustand v5 內建）
- PumpGrid、WaterLevelBar 等純展示子組件用 React.memo
- event handler 用 useStore.getState() 而非 hook

---
#2 339KB MP3 隨 bundle (config/stations.ts:49)

export const DEFAULT_ALARM_AUDIO_URL = './freesound_community-security-alarm-63578.mp3';

檔案在 public/ 目錄，每次 Vite build 複製到 dist/。瀏覽器 preload scanner 掃到 HTML 後發現 public/ 資源會啟動請求。Android WebView 的 Chromium 會對所有靜態資源做 preload，這個 339KB MP3 排在 JS/CSS 請求之後，佔用 HTTP connection pool。

影響: 首次載入時（尤其是慢網路或舊手機）這個音檔的請求阻塞其他更關鍵資源。且警報音從不應該和 app bundle 混在一起——使用者可能永遠不觸發警報，但每次啟動都要載這個檔。

修法: 不在 public/ 放音檔。改為警報首次觸發時 fetch 並 cache 為 Blob URL。或者乾脆已經有 base64 機制了，直接用 base64 decode（反正目前也有這個路徑）。

---
#6 Audio memory leak (audio.ts:20-29, tideSlice.ts:141)

// audio.ts
const audio = new Audio(base64);
audio.loop = true;
audioMap.set(stationno, audio);

// tideSlice — 潮汐警報播放
for (const alarm of tideReasons) {
  playStationAlarm(alarm.stationno, DEFAULT_ALARM_AUDIO_URL);
}

playStationAlarm 每次 new Audio()，即使同站重複觸發也是新建 HTMLAudioElement。loop = true 意味著 Audio 物件永久存活直到 stopStationAlarm。另外 stationAlarmAudios（使用者自訂 base64）被存進 localStorage，base64 體積是原始檔 4/3 倍，一個 300KB MP3 轉 base64 變 ~400KB，存 localStorage 5MB 限制下塞幾個就爆了。

影響: 長時間使用後記憶體漸增。多站同時警報（例如颱風天）可能建幾十個 Audio 物件。base64 在 localStorage 可能導致儲存失敗（超過 5MB），連帶設定也存不了。

修法: Audio pool reuse（同站不重建），base64 改存 IndexedDB 或限制檔案大小。

---
中嚴重度

#8 歷史水位 30 parallel HTTP (usePumpData.ts:59-71)

const stationNos = data.map((s) => s.stationno);
const results = await Promise.allSettled(
  stationNos.map((no) => fetchWaterLevelHistory(no, 2)),
);

fetchWaterLevelHistory 內部對每個站號雙 API 平行（主 + 備援），所以 15 站 = 30 個 HTTP POST 請求。每 5 分鐘一次。且不管使用者勾選幾站，都對全部 15 站請求。

影響: 網路頻繁，每 5 分鐘 30 個 POST 打到政府 server。如果使用者只選 3 站，27 站請求白費。server 可能 rate limit。

修法: 只對 selectedStations 請求。

---
#9 lastUpdateTime locale string 來回 parse

// dataSlice.ts — 存
lastUpdateTime: new Date().toLocaleString('zh-TW'),

// Header.tsx — 讀
const diff = Math.floor((Date.now() - new Date(lastUpdateTime).getTime()) / 1000);

存的時候格式化成 locale string（慢操作：locale negotiation + ICU format），讀的時候又 new Date(localeString).getTime() parse 回去。toLocaleString 比存 epoch 慢約 20-50 倍。

影響: 每次資料更新（30 秒一次）都做一次 format + 一次 parse。微小但累積。

修法: 存 Date.now() epoch，顯示時才 format。

---
#10 Header 每秒 clock interval 背景不停

const id = setInterval(tick, 1000);  // 無 cleanup 條件

每秒 setClock → Header re-render。App 在背景（visibilityState === 'hidden'）時仍每秒跑。RefreshTimer 同樣每秒倒數。

影響: 背景時無意義的 CPU + 電池消耗。每 1 秒觸發一次 React state update + re-render + reconciliation。

修法: visibilitychange 時暫停/恢復 interval。

---
#7 Array index 作 React key

// AlarmBanner.tsx:48
{alarm.reasons.map((r, i) => (<span key={i} ...>))}

// StationCard.tsx:192
key={`${r.type}-${r.pumpId ?? i}`}  // fallback 仍是 index

React 用 key 追蹤 element identity。當陣列中間元素被 dismiss 移除時，index 會位移——React 會誤判 element A 變成 element B，導致不必要的 DOM 操作或 state 錯亂。

影響: AlarmBanner dismiss 時可能出現短暫的動畫/文字錯亂。severity 中偏低但符合 OWASP/React best practice 違規。

修法: 每個 reason 生成穩定唯一 key。

---
#11 Biometric 程式碼重複 + callback leak

// LoginPage.tsx
window.__biometricResult__ = (ok) => { ... };
// useAuth.ts
window.__biometricResult__ = (ok) => { ... };

兩邊一模一樣邏輯。LoginPage 掛的 callback 在元件 unmount 時沒清理，留在 window 上。15 秒 timeout 內切頁（例如快速登出再登入），舊 callback 還在，新舊兩個 callback 可能都執行。

影響: 快速切換可能觸發雙重 callback，導致 race condition 或錯誤 alert。中偏低。

修法: 移到一個 utils 函數，cleanup 時設為 null。

---
#12 operationLog 無上限存 localStorage

pumpOperationLog: [...state.pumpOperationLog, newEntry],
gateOperationLog: [...state.gateOperationLog, newEntry],

兩陣列只 append 不 trim。localStorage 全域名空間 5-10MB 上限。颱風天大量機組啟停 + 閘門操作，一天可能累積數千筆。每筆包含 timestamp、stationNo、pumpId 等，JSON serialized。

影響: 長時間使用 localStorage 可能被塞爆，導致設定存檔失敗、Firebase session token 寫不進去。邊緣情況但後果嚴重（使用者突然被登出）。

修法: 設上限最近 500 筆。

---
#13 雙重 JSON.stringify (backgroundAlarm.ts:74-84)

const payload = JSON.stringify({
  selectedStations: JSON.stringify(params.selectedStations),
  stationOrder: JSON.stringify(params.stationOrder),
  // 每個欄位都各自 stringify 再包一層
});

各欄位先變成 string，再整包 stringify。Native 端 parse 兩層才能取到原始值。多一層 encode/decode + 體積變大（字串內 escape quote）。

影響: CPU 浪費 + payload 體積膨脹。中偏低。

修法: 一次 JSON.stringify(params) 就好，native 端一次 parse。

---
#1 缺 PWA manifest / meta tags (index.html)

<!-- 完全沒有 -->
<link rel="manifest" />
<meta name="theme-color" />
<link rel="icon" />

雖是 Capacitor APK，但也有 PWA 用途（瀏覽器直接開）。沒有 manifest 就無法添加到主畫面。沒有 icon & theme-color 影響基本外觀。

影響: PWA 體驗缺損。不影響 APK。

---
低嚴重度

#14 animate-scaleIn 缺少 @keyframes (index.css:21-22)

<!-- UpdateDialog.tsx -->
className="... animate-scaleIn"
/* index.css 沒有 @keyframes scaleIn */

Tailwind v4 不會自動為任意 animate-* 產生 @keyframes，結果這個 class 無效，動畫不顯示。

影響: UpdateDialog 出現時沒有縮放動畫。極低。

---
#15 tideSlice 用 stationData.find 取站名

const stationData = state.stationData.find(s => s.stationno === stationNo);
const name = stationData?.stationName;

STATION_NAMES 配置檔已有所有站號→站名對照表，不需要從 runtime data 取值。這個 find 建立 slice 間的隱性耦合（tideSlice 依賴 dataSlice 的狀態）。

影響: 如果 updateTide 在 setStationData 之前被呼叫，站名變成 undefined。目前時序上不會發生，但耦合脆弱。

修法: 直接用 STATION_NAMES[stationNo] ?? stationNo。

---
要我開始修嗎？從高嚴重度的 #3 (Firebase lazy) 和 #4 (subscribe selector) 開始最快見效。
---
你想先處理哪些？我建議從 #3 (Firebase lazy import) 和 #4 (subscribe selector) 開始，這兩個影響最大且改動最小。或者你想一次整理全部？

Still pending:
- #7 — AlarmBanner.tsx:50 仍用 key={i}（array index 作 React key）
- #14 — index.css 只有 slideDown 和 shake，沒有 scaleIn（UpdateDialog 的 animate-scaleIn 無效）
- #15 — tideSlice.ts:93 仍用 state.stationData.find(...) 取站名，沒有改用 STATION_NAMES

These are all low severity. Also #1 (PWA manifest) — doesn't affect APK, only browser PWA users.

想修這三個嗎？改動都很小。

Audit 結果出來。之前 15 項已修完 11 項（#2-#4, #6, #8-#13），還有 4 項 pending：

┌─────┬──────────────────────────────────────────────────────────────────────────────────┬────────┐
│  #  │                                       問題                                       │ 嚴重度 │
├─────┼──────────────────────────────────────────────────────────────────────────────────┼────────┤
│ #7  │ AlarmBanner.tsx 仍用 key={i}（array index），StationCard 也有 fallback to index  │ 低     │
├─────┼──────────────────────────────────────────────────────────────────────────────────┼────────┤
│ #14 │ index.css 缺 @keyframes scaleIn，UpdateDialog 的 animate-scaleIn 無效            │ 極低   │
├─────┼──────────────────────────────────────────────────────────────────────────────────┼────────┤
│ #15 │ tideSlice.ts 仍用 state.stationData.find() 取站名，已有 STATION_NAMES 常數卻沒用 │ 極低   │
├─────┼──────────────────────────────────────────────────────────────────────────────────┼────────┤
│ #5  │ StationCard 12 個 selector 可進一步合併（子組件已 memo）                         │ 中     │
├─────┼──────────────────────────────────────────────────────────────────────────────────┼────────┤
│ #1  │ 缺 PWA manifest/meta tags（不影響 APK）                                          │ 低     │
└─────┴──────────────────────────────────────────────────────────────────────────────────┴────────┘

加上一個新發現的低風險項：App.tsx useEffect deps 少了 handleKickedOut（實務上不會出問題因為 zustand selector 是 stable reference）。

要修哪些？#7 和 #14 和 #15 都是兩三行的小改動。#5 需要合併 selector 稍微多一點工。#1 不影響 APK 使用。
